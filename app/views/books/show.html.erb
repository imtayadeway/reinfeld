<h1><%= link_to "All Books", root_path %>
<h1><%= @book.name %></h1>
<ul>
  <% @book.chapters.each do |chapter| %>
    <li>
      <%= link_to chapter.name, chapter_path(chapter) %>
    </li>
  <% end %>
</ul>
