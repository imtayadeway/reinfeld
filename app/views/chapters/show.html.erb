<h1><%= @chapter.name %></h1>
<ul>
  <% @puzzles.each do |puzzle| %>
    <li>
      <%= link_to puzzle.id, puzzle_path(puzzle) %>
      <div class="board" id="<%= puzzle.id %>" start-position="<%= puzzle.position %>" to-move="<%= puzzle.to_move %>" style="width: 180px"></div>
    </li>
  <% end %>
</ul>
<%= javascript_include_tag "chess/chapter" %>
